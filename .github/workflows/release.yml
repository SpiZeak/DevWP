name: Build and Release Tauri App

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - platform: 'macos-latest' # for Arm based macs (M1 and above)
            args: '--target aarch64-apple-darwin'
            rust-targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
          - platform: 'macos-latest' # for Intel based macs
            args: '--target x86_64-apple-darwin'
            rust-targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
          - platform: 'ubuntu-24.04'
            args: ''
            rust-targets: ''
          - platform: 'ubuntu-24.04-arm'
            args: ''
            rust-targets: ''
          - platform: 'windows-latest'
            args: ''
            rust-targets: ''
          - platform: 'windows-latest' # for ARM based Windows
            args: '--target aarch64-pc-windows-msvc'
            rust-targets: 'aarch64-pc-windows-msvc'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache Bun dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src/tauri -> target'

      - name: Cache Tauri bundler tools
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/tauri
            ~/AppData/Local/tauri
          key: ${{ runner.os }}-tauri-tools-v1

      - name: Install dependencies (ubuntu only)
        if: startsWith(matrix.platform, 'ubuntu-')
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xdg-utils libfuse2

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPIMAGE_EXTRACT_AND_RUN: 1
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'DevWP ${{ github.ref_name }}'
          releaseBody: |
            ## Installation

            ### Linux
            - **AppImage** (recommended, x86_64): Download `DevWP_x.x.x_amd64.AppImage`, make it executable (`chmod +x`), and run it.
            - **Debian/Ubuntu (x86_64)**: Download `DevWP_x.x.x_amd64.deb` and install with `sudo dpkg -i DevWP_x.x.x_amd64.deb`.
            - **AppImage (ARM64)**: Download `DevWP_x.x.x_aarch64.AppImage`, make it executable (`chmod +x`), and run it.
            - **Debian/Ubuntu (ARM64)**: Download `DevWP_x.x.x_arm64.deb` and install with `sudo dpkg -i DevWP_x.x.x_arm64.deb`.

            ### Windows
            - **x64**: Download `DevWP_x.x.x_x64-setup.exe` and run the installer.
            - **ARM64**: Download `DevWP_x.x.x_arm64-setup.exe` and run the installer.

            ### macOS
            - **Apple Silicon (M1/M2/M3)**: Download `DevWP_x.x.x_aarch64.dmg`, open it, and drag DevWP to Applications.
            - **Intel**: Download `DevWP_x.x.x_x64.dmg`, open it, and drag DevWP to Applications.

            > **Requirement**: [Docker Desktop](https://www.docker.com/products/docker-desktop/) must be installed and running.
          releaseDraft: false
          generateReleaseNotes: true
          prerelease: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name,
            '-alpha') }}
          args: ${{ matrix.args }}
          projectPath: src/tauri
